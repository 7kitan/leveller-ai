services:
  frontend:
    image: ${GHCR_FRONTEND_IMAGE}
    container_name: advisor_frontend_prod
    restart: always
    ports:
      - "3000:3000"
    networks:
      - advisor_net_external

networks:
  advisor_net_external:
    external: true
    name: advisor_net_prod
